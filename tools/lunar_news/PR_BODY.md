This update archives recent NASA lunar-base source leads as readable briefings and structured simulation research actions.

Review `research/news/briefings/` for coverage and failures, then inspect the linked records. Automated keyword routing provides candidate research tasks; it does not extract verified engineering parameters. Check primary sources, relevance, duplicates, and conflicts before using the evidence.

Collector tests, schema validation, repository target checks, and repository structural checks ran before publication. PRs opened with GitHub's built-in token may not trigger another workflow; the publishing job performs these checks explicitly.

Merging preserves the news archive only. Simulation changes require a separate evidence review and model PR. No model parameters are changed by this update.
